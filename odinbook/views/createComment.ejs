<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
        <% posts.forEach(post =>  { %>  
            <p><%= post.content %></p>
            <form action="/comment" method="post">
              <input type="hidden" name="postId" value="<%= post.id %>">
              <label for="id">Id</label>
              <input type="text" name="id">
              <br><br>
              <label for="content">CONTENT</label>
              <input type="text" name="content" placeholder="Write a comment..." style="width: 600px; border-radius:20px; background-color: lightgray;">
              <br>
              <button type="submit">Submit</button>
            </form>
                <% comments.forEach(comment =>  { %>
                  <h2><%= comment.username %></h2>
                  <form action="/createComment/<%= (comment.id) %>/edit" method="POST" style="display:inline;">
                    <textarea name="comment"><%= comment.comment %></textarea>
                    <button type="submit">Save Changes</button>
                    <i class="fa fa-retweet"></i>
                    <i class="fa fa-heart-o"></i>
                  </form>
                  
                  <form action="/createComment/<%= comment.id %>/delete" method="POST" style="display:inline;">
                    <button type="submit" onclick="return confirm('Are you sure you want to delete this comment?');">Delete</button>
                  </form>
    
                <% }); %>   
          <% }); %>    
    </form>     
</body>
</html>