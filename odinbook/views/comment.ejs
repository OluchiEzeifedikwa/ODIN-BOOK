<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body class="post-body">
    <div class="post-container">
        <div class="board" id="dashboard"></div>
          <div class="board" id="profile">
            <h1 class="post-header">YOUR COMMENT</h1>
            <br>
            <% post.comments.forEach(comment => { %>
                <p style="color: black;"><%= comment.content %></p>
            <% }); %>
            <a href=""><i class="fa fa-heart-o"></i></a>
            <a href=""><i class="fa fa-comment-o"></i></a>
            <a href=""><i class="fa fa-bookmark-o"></i></a>
            <form action="/posts/<%= post.id %>/comments" method="post">
              <input type="hidden" name="postId" value="<%= post.id %>">
              <br>
              <input type="text" name="content" placeholder="Write a comment..." style="width: 200px; border-radius:20px; background-color: lightgray;">
              <br>
              <button type="submit">Submit</button>
            </form>
          </div>
    </div>    
</body>
</html>